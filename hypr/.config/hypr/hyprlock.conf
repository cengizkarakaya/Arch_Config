general {
    hide_cursor = true
}

background {
    monitor =
    color = rgba(20, 20, 20, 1.0)
}

label {
    monitor =
    text = $TIME
    font_size = 72
    position = 0, 120
    halign = center
    valign = center
}

label {
    monitor =
    text = $USER
    font_size = 20
    position = 0, 30
    halign = center
    valign = center
}

input-field {
    monitor =
    size = 300, 50
    position = 0, -60
    halign = center
    valign = center

    dots_center = true
    fade_on_empty = false
    placeholder_text = Parola
}
